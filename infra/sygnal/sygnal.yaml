# Sygnal Push Gateway Configuration for SynapseChat
# Minimal dev config - no push providers configured yet

db:
  path: /config/sygnal.db

http:
  port: 8200
  bind: '0.0.0.0'

log:
  level: INFO
  format: '%(asctime)s - %(name)s - %(lineno)d - %(levelname)s - %(message)s'

apps: {}