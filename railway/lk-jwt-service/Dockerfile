FROM ghcr.io/element-hq/lk-jwt-service:latest

# lk-jwt-service runs on port 8080 by default
# Environment variables are set via Railway
EXPOSE 8080

# No CMD needed — the image has a default entrypoint